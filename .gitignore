# Each sub-project keeps its own .gitignore (mobile/.gitignore,
# firmware/.gitignore) for tool-specific artifacts. This root file only
# covers things that could land at the repo root regardless of editor/OS.

.DS_Store
Thumbs.db
*.log
