#ifndef SECRETS_H
#define SECRETS_H

// Copy this file to `secrets.h` (already in .gitignore — never commit real
// credentials, rule.md section 2/3) and fill in real values.
//
//   cp include/secrets.h.example include/secrets.h

constexpr const char *WIFI_SSID = "your-wifi-ssid";
constexpr const char *WIFI_PASSWORD = "your-wifi-password";

// HiveMQ Cloud (architecture.md section 3) — TLS on port 8883.
constexpr const char *MQTT_HOST = "xxxxxx.s1.eu.hivemq.cloud";
constexpr const int MQTT_PORT = 8883;
constexpr const char *MQTT_USERNAME = "device-room-01";
constexpr const char *MQTT_PASSWORD = "your-mqtt-password";

#endif // SECRETS_H
