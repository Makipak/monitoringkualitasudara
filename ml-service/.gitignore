.venv/
__pycache__/
*.pyc
.env

# Trained model artifacts - not committed, drop the matching pair onto
# the VPS manually (see deploy/README.md step 3, ml-service/README.md
# "Files"). Keep MODEL_PATH/SCALER_PATH in app.py pointed at whichever
# timestamped pair is actually present.
*.pkl
*.keras

# Generated by `jupyter nbconvert --to script Falhore.ipynb` to run
# training locally instead of in Colab (see ml-service/README.md and
# dl-prediction-model memory) - a mechanical export of the notebook, not
# a maintained source file; regenerate it from Falhore.ipynb rather than
# editing it directly or committing it.
train_local.py
training_artifacts/

# Intermediate combined dataset the notebook writes out (cell-0,
# df.to_csv(...)) - regeneratable from the 3 source CSVs, not a source
# file itself.
dataset_full9_v2.csv
